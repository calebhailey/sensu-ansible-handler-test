---
- name: start nginx
  hosts: nginx
  remote_user: centos
  become: yes
  become_method: sudo
  become_user: root
  tasks:
  - name: restart nginx
    service:
      name: nginx
      state: started
